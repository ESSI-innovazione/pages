# TimeVision pages (loader)

This repository holds only a login screen and a small loader. It contains no
internal content. Page content lives in a private repository and is fetched
only after signing in with a @timevision.it email and the team password.

A page link looks like `https://essi-innovazione.github.io/pages/<name>_<code>`.
